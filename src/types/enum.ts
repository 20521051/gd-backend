export enum Gender {
  Male = 'male',
  Female = 'female',
  Other = 'other',
}

export enum Job {
  Teacher = 'Teacher',
  Doctor = 'Doctor',
  Student = 'Student',
  Other = 'Other',
}

export enum Degree {
  BSc = 'BSc',
  Engineer = 'Engineer',
  Master = 'Master',
  Doctor = 'Doctor',
  Reader = 'Reader',
  Professor = 'Professor',
}

export enum modelTypes {
  Course = 'course',
  Video = 'video',
  Blog = 'blog',
}
